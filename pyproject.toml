[project]
name = "textmetrics"
version = "0.1.0"
description = "Tiny text statistics library"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["uv_build>=0.9.0,<0.13.0"]
build-backend = "uv_build"
